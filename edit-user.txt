FUNCTION edit_user(username, new_details):
    IF username IS EMPTY THEN RETURN FALSE
    user = FIND_USER(users_list, username)
    IF user IS NULL THEN RETURN FALSE
    user.details = new_details
    SAVE_TO_STORAGE(users_list)
    RETURN TRUE